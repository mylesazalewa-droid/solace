# Copy to .env.local and fill in. Never commit .env.local.
# The Google OAuth *desktop* client secret for one-click "Continue with Google".
# Get it from Google Cloud Console -> APIs & Services -> Credentials -> your Desktop OAuth client.
MAIN_VITE_GOOGLE_CLIENT_SECRET=
